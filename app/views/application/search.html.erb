<% if @releases == "Your search has found nothing" %>
  <div class="row">
    <h1 class="small-centered"><%=@releases%></h1>
  </div>
<%else%>
  <div class="row">

    <h1 class="small-centered">Search Results for <%= params[:search] %></h1>
  </div>
  <div class="row small-up-1 medium-up-2 large-up-3">
      <% @releases.each do |release| %>
    <div class="search-tile">
      <div class="search-tile-img">
      <%= link_to image_tag(release.album_art_url), release_path(release) %>
      </div>
      <div class="search-tile-text">
      <%= link_to release.title, release_path(release)%> - <%= release.artist%>
      </div>
    </div>
    <% end %>
  <% end %>
  </div>

  <div id="app">

  </div>
